Now I want you to add a feature next to the page title. A small button just like the attachment and delete button and copy paste button. I want a small button and this button will, if I click it, it should display a template manager. A template manager is a JavaScript and CSS pop-up that will pop up large and draggable and scalable. It should be designed very well and very well structured and constructed. What it will do, it will display the data in a hierarchy level where the user can edit each node with its description in this system. So it a JavaScript CSS HTML pop that will resemble a database editing environment where it takes all the page and put it in folders as parents and files as children and grandchildren if there are two files. Okay, so it will manage this page, make it larger, clearer, put an icon of a microscope or something like this to denote what this is about try to make it cleverly and for each node you should add a description next to it where it's added. After the user is finished he will put save and it will push everything according to the design template into the page contain or not. That's it. Nothing will be touched in the database. 