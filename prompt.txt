Also, I want in the prompt editor a nice clever search bar just next to the prompt editor title, cleverly done and created, compatible with the layout and the theme. And it will search everything inside the prompt editor by filtering the prompt editor directly. And put a reset so I can clear out the search or clear search or whatever. So put a button that I can default or reset the search to nothing. and it should work in Ajax or in dynamic search. So whenever I click one letter, it will search from the text and filter it inside the prompt editor. Make it clever and dynamic and without any bugs so I can add more functionality later on. 