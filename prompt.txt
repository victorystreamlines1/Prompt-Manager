Now I want you to figure out the localhost root. It might be set on any root application. And put it above the container, above the path container, as ghost. So if I change the localhost, it will change the ghost text above the localhost selected path. Dynamically, it should change if I change my localhost from Laragon or Apache or any other server. So it will detect the host folder from the system on my PC. And put it as a ghost text above the container. Let me see it. 