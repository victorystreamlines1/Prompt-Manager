Now I want to add a facility in the iFrame. You can see the path, it says http slash localhost. I want to add whatever sub file inside the localhost. host which is the selected main host directory showing in the path but not the main directory make sure it's not the main directory which is in this case it's AI from the dictionary or prompt manager or whatever the local host will take the place of the main directory but the sub file or sub file under a sub folder should be shown whenever I navigate to that specific file or sub file or sub folder. According to the choice selection, the path should display the path. Make sure the main directory is shown or displayed in the path as localhost. and not shown as the name of the directory according to the best practice 