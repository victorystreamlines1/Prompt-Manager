Okay, but I tried to create a new project. It did not tick. It did not save it. Also, it's not showing in the drop down. The screen's freeze. Make sure you're compatible with the system of our database connection. The database connection system is a toggle. It toggles between remote, and you'll find it on the upper right, area of the page a remote that toggles from remote to local it does not go to local if it's in the localhost because this local is meant to be hosting as local so it should be on the remote therefore make sure that it follows the connection in the upper right which is on the remote now I'm from the server I'll try it from the other screen as local. Let me see. Maybe it should be anyway. It should work from remote in all cases. 