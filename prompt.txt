Remove this timer completely from prompt editor. You'll find it in the header of the prompt editor dashboard. I'll give you HTML, just remove it completely. ----><div class="auto-send-timer snipcss-HsWNs" id="autoSendTimer" title="Auto-send interval (0 = disabled)">
    <i class="fas fa-sync-alt"></i>
    <input type="number" class="timer-input" id="timerInput" min="0" max="999" value="0" onchange="updateAutoSendTimer()" oninput="updateAutoSendTimer()" fdprocessedid="ore46m">
    <span class="timer-label">sec</span>
    <span class="timer-countdown" id="timerCountdown">0</span>
</div>
