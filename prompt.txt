Now I want to add a new feature, and it is the feature of moving a folder from one directory to another directory. So it will move the folder with all its subfiles and folders inside that folder. also when it moves it it should delete it from the place where it moved it from and put it in the destination of this moving place put put the moving tools in between The two set of buttons. In the bottom horizontal menu bar of the prompt editor. The one on the left is select file, pull and push button. And one on the right is the set of buttons of creating files and folders and deleting them and renewing them. In between these, put the Move folder Tools. 