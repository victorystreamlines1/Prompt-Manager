There is a problem when I generate the generated text from design enhancer. You see in the development dashboard the button generate. When I click it any page container that has detailed files does not show properly it get messed up the pages that have just one node and doesn't have multi-file with descendant and more than one level it shows properly but if it has directories and subdirectories and files and subfiles it doesn't show properly it gets messed up make sure when it puts it it puts it in a way that it is readable all the lines are messed up and they're not set properly because there are more than one node inside that container And I think this one is pushed from the design enhancer from push all to dashboard the project file uploader when it uploads the whole set of of files and directories into one node or one container as one item from the pages when it generates it does not generate properly so do not change any other normal node with the title and description, but change the ones that are pushed from push all to dashboard from the project file uploader. Make sure when I generate and click on the generate button, the green one, in the development dashboard container, it generates properly, specifically the one that is pushed as a bulk of parent and child nodes. 