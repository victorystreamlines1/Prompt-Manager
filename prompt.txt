You are working on an existing PHP-based web application called Prompt Manager.

I need you to add a brand new top-level section/tab/mode to the main interface.

IMPORTANT CONTEXT:
- The application already has two top sections in the upper main navigation area:
  1) Workspace
  2) iFrame Scalar
- I want you to add a THIRD section after them.
- This third section must be COMPLETELY differentiated from the other two sections in purpose, layout, styling emphasis, and behavior.
-this new section will be displayed normally in between the two side vertical panels like the previous two sections so it is different but it will take the normal layout of being    displayed between the two vertical panels the left and the right of this application 
- The new section is NOT just another iframe area.
- It is a controlled visual preview environment for a live visual editing workflow.
- The page being previewed is NOT foreign. It is my own application/page/route, under my control, and I own the code and structure.
- My Prompt Manager is already connected to my IDE workflow such as Windsurf / Cursor / GitHub-related project structure.
- I want the user to visually inspect a page, select an element, understand its route/component/file mapping, and then use Prompt Manager to send the correct instructions to the IDE AI.
I want the user to be able to detect parts live on the preview container and also to put his prompt directly on that container. And then using the connection, it will send it to the IDE he is connected on by the connection and my prompt manager, which is the button folder that connects directly to the IDE. 
-the instructions will be sent through prompt.text according to the application mechanism of prompt manager application  
- The system is PHP-based, so implement the application structure in PHP, but use JavaScript where needed for browser-side interactions. Do NOT try to do browser live DOM interaction in PHP only.

YOUR TASK:
Design and implement a new section called:
Visual Preview

If needed, you may use the title:
Visual IDE Preview

Use the better naming choice in the UI, but keep the functionality as described below.

==================================================
1. HIGH-LEVEL GOAL
==================================================

Add a third mode/tab in the top interface navigation, placed AFTER:
- Workspace
- iFrame Scalar

The new third tab must open a completely dedicated visual preview mode that allows:
- loading a controlled preview route/page from the user’s own project
- visually displaying the page in a preview pane
- switching into inspect/select mode
- highlighting hovered elements
- selecting clicked elements
- capturing metadata from the selected element
- showing route/path/file/component/node mapping
- generating structured context that Prompt Manager can use to send commands to the IDE AI
- enable the user to select parts and to add a prompt regarding this part and then send it live from the preview into prompt.text IDE dialog box to start processing the prompt request. 

This must feel like a visual IDE assistant area, not a generic iframe viewer.

==================================================
2. UI/UX REQUIREMENTS
==================================================

A. Top Navigation
Add a new third section in the same top navigation group where:
- Workspace
- iFrame Scalar
already exist.

Order must become:
1) Workspace
2) iFrame Scalar
3) Visual Preview

The new tab must:
- visually fit the current neon/dark/cyber UI style
- have a distinct accent color or glow identity that differentiates it from the other two
- look intentional and first-class, not like a temporary add-on
- preserve the visual language of the current Prompt Manager theme

B. Main Layout Behavior
When the user clicks Visual Preview:
- the main central content area must switch into a dedicated preview workspace
- it should NOT simply reuse the normal Workspace layout without adaptation
- it should feel like entering a different mode of operation

The layout should include:

1) Preview Header / Control Bar
2) Main Preview Canvas
3) Selection / Inspector Information Panel
4) Action Bar for Prompt / IDE Sync

C. Preview Header / Control Bar
At the top of the Visual Preview section, add a clear toolbar containing at minimum:

- Preview title
- Current project indicator
- Current route / path field
- Load button
- Reload button
- Inspect toggle
- Select mode toggle
- Sync to IDE button
- Open in full view button
- Status badge (Ready / Inspecting / Synced / Error)

Optional but recommended:
- device mode switcher (desktop / tablet / mobile)
- zoom control
- fit-to-screen button
- route dropdown or input

D. Main Preview Area
This is the core visual area.

Requirements:
- must render the target page/route from the user’s own controlled application
- do not treat this as an external unknown site
- build this as a controlled preview route/view
- the preview should be large and central
- the preview area should support a visual overlay layer
- the preview area should support hover outline and selected outline
- the preview should be optimized for element selection and visual understanding

Important:
Do NOT reduce this to a simple iframe-only feature.
If you use an iframe internally for isolation or rendering convenience, it must be a controlled app-owned preview and wrapped in a full visual editing system.
The UI must present it as a Visual Preview environment, not just “an iframe box”.

E. Inspector Side Panel
Add a dedicated information panel for the selected element.

This panel must show:
- current route/path
- selected element tag
- element text snippet
- data-node-id if available
- component name if available
- file path if available
- CSS selector
- state/flags if relevant
- editable attributes if relevant
- prompt context summary
- action buttons

Action buttons should include:
- Copy Selection Data
- Send to Prompt
- Send to IDE
- Clear Selection
- Re-inspect

F. Prompt Action Area
Inside Visual Preview mode, add an action area that helps translate selection into AI instructions.

This area should include:
- a text area or structured prompt box
- a generated context block
- one-click actions to:
  - create prompt from selected element
  - append selection to current prompt
  - send structured prompt to IDE connector
  - save selection context

==================================================
3. DESIGN DIFFERENTIATION REQUIREMENTS
==================================================

The new section must be completely differentiated from Workspace and iFrame Scalar.

That means:
- different visual emphasis
- different icon
- different status language
- different internal layout hierarchy
- clearly focused on “visual inspection + live preview + IDE sync”

Do NOT make it feel like a duplicate of Workspace.
Do NOT make it feel like a mobile simulation clone.
This is a visual IDE bridge section.

Suggested visual identity:
- use a cyan / teal / electric green accent family, or another clearly distinct accent
- use a dedicated icon such as eye / layers / inspect / monitor / cursor-target
- use stronger framing around the preview canvas
- use glass/neon borders consistent with the current style

==================================================
4. TECHNICAL ARCHITECTURE REQUIREMENTS
==================================================

The application is PHP-based.
Implement the feature cleanly in a PHP-friendly architecture.

Use:
- PHP for server routes, templates, state endpoints, and backend logic
- JavaScript for browser interactions such as hover, selection, DOM reading, overlay rendering, and async sync actions
- CSS consistent with current theme

Create a proper structure, not a hack.

Suggested architecture:
- a dedicated PHP view/template for Visual Preview mode
- dedicated JS file(s) for preview interactions
- dedicated CSS file(s) if needed
- API endpoints for selection state and IDE sync actions

Suggested file organization:
- /visual-preview.php or integrated section renderer
- /api/preview/load.php
- /api/preview/select.php
- /api/preview/state.php
- /api/preview/send-to-ide.php
- /assets/js/visual-preview.js
- /assets/js/visual-preview-inspector.js
- /assets/css/visual-preview.css

You may adapt this structure to the project’s existing architecture, but keep the implementation modular and maintainable.

==================================================
5. PREVIEW LOADING MODEL
==================================================

The preview target is a page/route that belongs to the user’s own application/project.

Implement a controlled preview model.

The system must support:
- choosing a route/path
- loading the corresponding preview page
- tracking the loaded route
- showing the route in the toolbar
- refreshing the preview without breaking the mode

Do NOT assume a foreign site.
Do NOT design around cross-origin restrictions as the primary case.
This is an app-owned page.

If needed, use a wrapper or controlled preview renderer so that the page can expose metadata and interact safely with Prompt Manager.

==================================================
6. ELEMENT SELECTION SYSTEM
==================================================

Implement a proper visual inspection system.

Requirements:
- hover highlights visible target elements
- click selects the element
- selected element stays highlighted
- selection information is pushed to the inspector panel
- selection must be cancelable/resettable
- selection state should be visually obvious

When an element is selected, collect as much of the following as available:
- current route
- tagName
- text content snippet
- id
- classes
- data-node-id
- data-component
- data-file
- nearest meaningful container
- CSS selector
- element bounds if useful
- timestamp

If the previewed page includes special metadata attributes such as:
- data-node-id
- data-component
- data-file
then use them.

If not, build a graceful fallback based on available DOM information.

==================================================
7. METADATA MAPPING STRATEGY
==================================================

This feature must support future AI-driven editing workflows.

So design the system to prefer explicit mappings such as:
- data-node-id
- data-component
- data-file

If these attributes exist on elements, use them directly.

If these attributes do not exist:
- derive a best-effort CSS selector
- collect parent/ancestor context
- collect visible text snippet
- collect semantic hints

The goal is to connect:
what the user sees
to
what file/component/element the IDE AI should modify.

==================================================
8. INSPECTOR PANEL DETAILS
==================================================

Build the inspector panel in detail.

Suggested sections:

A) Selection Summary
- selection status
- element tag
- short description
- route/path

B) Technical Mapping
- component
- file path
- node id
- selector
- classes
- id

C) Content Snapshot
- visible text
- attributes
- editable hints

D) AI Prompt Context
Generate a structured context preview like:
- Route: /dashboard
- Element: button
- Text: Save
- Component: SettingsForm
- File: /components/settings-form.php
- Node ID: settings.save_button

E) Actions
Buttons:
- Copy JSON
- Copy Prompt
- Send to Prompt Editor
- Send to IDE
- Clear Selection

==================================================
9. PROMPT GENERATION BEHAVIOR
==================================================

Inside the Visual Preview section, add logic that can generate a structured prompt snippet based on the selected element.

Example output style:
- current route
- selected element type
- text/label
- file/component mapping
- requested change placeholder

Make the prompt generation clear, structured, and optimized for AI coding assistants.

Example intent:
“On route /dashboard/settings, update the selected primary button with text ‘Save Changes’, mapped to component SettingsForm and file /components/settings-form.php. Modify styling to make it larger, more prominent, and consistent with the current neon theme.”

This should be editable by the user before sending.

==================================================
10. IDE CONNECTOR AREA
==================================================

Add a visible action path for sending context to the IDE connector.

This does not need to fully implement external IDE APIs unless the project already supports them, but the UI and internal hooks must exist.

Requirements:
- button for Send to IDE
- button for Append to Prompt Editor
- status feedback on success/error
- structured payload preparation

If IDE connector functionality already exists in the project, integrate with it.
If not, prepare the code in a clean way so it can be connected later.

==================================================
11. RESPONSIVE AND DISPLAY MODES
==================================================

The Visual Preview section should support:
- desktop preview mode
- mobile preview mode if possible
- fit-to-container behavior
- scroll-safe preview
- stable overlay positioning

Do not break the existing iFrame Scalar section.
Do not merge their roles.
Keep Visual Preview distinct.

==================================================
12. IMPLEMENTATION QUALITY REQUIREMENTS
==================================================

Implement this feature properly and cleanly:
- no broken layout
- no placeholder-only implementation
- no superficial static panel
- no fake buttons without structure behind them
- no messy inline scripts unless the codebase already uses them that way
- preserve the current design system
- preserve existing functionality
- do not break Workspace or iFrame Scalar
- use reusable components/functions where appropriate
- keep naming clean and understandable

==================================================
13. PHP IMPLEMENTATION EXPECTATIONS
==================================================

Since this project uses PHP:
- use PHP includes/templates/components where appropriate
- integrate the new section into the current page rendering structure
- wire the new tab into the existing navigation logic
- make the Visual Preview section load conditionally based on the active mode/tab
- use proper backend endpoints for async interactions if needed
- sanitize route/path values
- keep server-side code maintainable

==================================================
14. JAVASCRIPT IMPLEMENTATION EXPECTATIONS
==================================================

Use JavaScript for:
- hover detection
- click selection
- overlay rendering
- inspector updates
- async fetch requests
- prompt payload generation
- state sync

Make the JS modular and easy to expand.

==================================================
15. STYLING EXPECTATIONS
==================================================

Keep the existing cyber/neon/dark style.

But make Visual Preview feel distinct by:
- giving it its own accent identity
- clear header bar
- strong canvas framing
- elegant inspector panel
- polished buttons and badges
- premium visual separation

Do not make it visually noisy.
Make it advanced, clean, and intentional.

==================================================
16. WHAT TO DELIVER
==================================================

Implement the feature fully in the codebase.

At the end, provide:
1) the new navigation integration
2) the new Visual Preview section
3) the preview toolbar
4) the preview canvas area
5) the inspector panel
6) the prompt action block
7) the JS interaction logic
8) any PHP endpoints/files added
9) a summary of what was created and where

If any assumptions are required, choose the most logical implementation based on the current application structure and keep it extensible.

IMPORTANT FINAL RULES:
- Do not replace Workspace.
- Do not replace iFrame Scalar.
- Add Visual Preview as a third top-level section after them.
- Make it clearly different from both.
- Build it as a real visual IDE bridge, not as a plain iframe viewer.
- Respect the current UI style and make the new section feel native to the app.
- Use PHP as the main project language and JavaScript only where browser interaction is necessary.