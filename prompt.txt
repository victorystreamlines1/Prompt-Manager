It is deleting the contents inside the folder, but not deleting the folder. It succeeded in deleting part of the folder, which is the content of the folder. But the folder itself is not being deleted. Therefore, make sure even if the folder is empty, it deletes the folder itself. Let alone the deletion of the content inside the folder. Now it is deleting whatever inside the folder, but this is not enough. It should be all together, the folder and whatever content inside the folder. 