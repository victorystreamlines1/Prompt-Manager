Now I want you to add a new functionality for the select file on the left side, the utmost left and the utmost right. In addition to push and pull, this push and pull is a complete overwrite. I want push append and push prepend Also Pull append And pull prepend For both select files So whenever I push it does not push an overwrite It pushes according to the type of push or pull I decide So you have four buttons will be added on each side so all together eight buttons one on the right select file and one on the left select file make sure the functionality work accordingly okay 