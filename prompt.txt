Now I want to do the same exact mechanism for the non-static nodes in the prompt templates. You'll find it in the vertical left dashboard of prompt manager, a panel vertically. you will find in it static built-in nodes and dynamic saved nodes. The dynamic saved nodes, I want to add the same exact mechanism, but the arrows should be to the right and to the left, because the location of the panel is to the left. so whenever it pushes the arrow should point at the prompt editor on the right when it pulls it will point at the left away from the prompt editor to denote that it is pulling and make the same exact mechanism of confirming the pull was successful. 