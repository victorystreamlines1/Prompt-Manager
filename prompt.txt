now I want you to fix the design of the saved prompts some space is not optimized very well so I want you to put the saved prompt title with the search box down under the row of select all container under it and cancel the row where the save prompts are now in and keep it with the second row also put the title in the beginning of the row save prompts then select all then the search box in one row which is the row where the select all container is and cancel the upper container which is unoptimized. Also make the functionality of the search. Search also the static section. Now it only search the dynamic section. 