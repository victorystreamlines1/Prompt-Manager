okay now I want the toggle to split whenever I toggle to image it will show both the image and the code if I put it on the code side it will only show the code so when I put it on the image it will split the container with a vertical border. The image part will be on the right quarter of the container. Three quarters will be for the code part. If I paste any image in the code part, it will be pasted as code. If I paste anything in the image part it will be pasted as image and the image should be aligned properly and nicely also if I paste code or text or normal text in the code area section it will display normally as text or file type okay I think it's very clear so again if I paste in the code area an image it will detect it and paste it as code if I type or paste into the code area text it will display text in the code area which is 3 quarters of the left part of the analytic container if I paste anything other than image in the image area which is the quarter right quarter it will not show the only thing will show is image and it will show in its normal image mod 