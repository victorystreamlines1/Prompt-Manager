now I want you to modify the feature in generate functionality button the generate I don't want you to change anything other than the display of the database the database should display according to the selection if the remote button is selected then it will display the remote connection if the local is selected then it should display the local if both are selected it should display both after generation of course and finally if nothing is selected when it generates it shouldn't generate any connection 