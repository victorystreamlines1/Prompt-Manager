Now I want you to apply this feature on both containers. Project prompts and prompt editor. I repeat. I want you to apply this feature in both containers. Project prompts and prompt editor. I want it to be resizable according to the pasted or written text inside the containers. so it resizes by expanding or shrinking according to the size of the text within the container area and its default is the original location so if the text is less than the default it will shrink into the default If the text is larger than the default, then it will expand according to the text. In other words, I want a molding package or container according to the text size. 