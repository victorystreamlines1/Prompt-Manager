Okay now I want you to list in the drop-down list you made connection with all the tables of the database I selected the drop-down. and put a checkbox in front of each table to be selectable. So I can push it to the description with the database information. So if I click it or check it as a selection or check multiple tables, it will send the database credentials and information with the structure and the name and information of the tables I select. If I don't select then it will only send the database So and also put the tables in a window scrollable window that doesn't take a lot of height and fits with the width of the container make it elegant and smart and scrollable so it doesn't take a lot of the height of the container contains the drop-down list. 