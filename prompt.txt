In the development dashboard, add another text area specifically for the selected project. In other words, this is connected to the database information. It should be a field, text area field. A general text area field, not specifically for pages. It will be along the whole container of the development dashboard. It's not the prompt editor. It's just part of the project's information table. So it's a field. And make it under the development dashboard containers of pages. and above the bottom menu with the project drop down and new save and load so just put it in there nice and clean try to put it artistically and make it draggable and not very big but small and draggable and additable I can add to it whenever I generate it will generate everything just like abnormal when I generate the button but it will push the text area inside the prompt editor. So it's part of the project that we select from the drop-down list projects that are listed and enrolled inside the table and database. 