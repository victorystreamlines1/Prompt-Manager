Add another push all button in the design enhancer. But this one will do the same exact thing but only to do two exceptions. one for the project file uploader it will click instead of push to prompts it will click on push to dashboard also when it comes to page creator it will not click on push to prompt like the previous button but it will click on push to dashboard the rest will be the same it will be normally pushed to the prompt only these two is an exception for this new button 