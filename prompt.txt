There is a problem in the drop-down selection of the projects. I think whenever I select a project, it clicks on generate automatically, so it generates some production in prompt editor. And this should not happen. Whenever I select, nothing should happen. It will only recall from the database table. the information and put it in the development dashboard, that's it unless I create, I put generate then it will generate in the prompt editor 